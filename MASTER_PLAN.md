# 🏗️ NABULINES ACADEMY — Master Build Plan

> **From Creator to Builder** — پلتفرم آموزشی خودکار وایب کدینگ
> نسخه: 1.3 (English-only · دیزاین مونوکروم قفل‌شده روی nabulines.com) | آماده‌ی اجرا در Claude Code | استک: Next.js + Cloudflare + Remotion + ElevenLabs
> 🌐 زبان کل محصول (ویدئو، صوت، زیرنویس، UI، اخبار): **انگلیسی** — بازار جهانی

---

## 🎯 ویژن

سایتی که یک کریتور بدون هیچ دانش فنی واردش میشه و در انتهای مسیر، خودش سایت میسازه، اپ لانچ میکنه و ابزار میفروشه. کل محتوا (متن، ویدئو، زیرنویس، کوییز، اخبار) توسط پایپ‌لاین ایجنتی **به زبان انگلیسی** تولید میشه — نقش انسانی فقط **تأیید نهایی از تلگرام** است.

> این سند مرجع (برای تو) فارسیه؛ تمام خروجی‌های پابلیک — syllabus، اسکریپت‌ها، نریشن، متن سایت، اخبار — انگلیسی تولید میشن.

**قوانین سخت پروژه (Hard Rules):**
1. ⏱️ هیچ ویدئویی بیشتر از **۵ دقیقه** نیست. محدودیت در سطح ژنراتور enforce میشه: اسکریپت انگلیسی حداکثر **۷۵۰ کلمه** (نرخ گفتار ~150 wpm).
2. 🚫 **Never-Fabricate:** هر دستور، کد یا ادعای فنی داخل درس باید قبل از پابلیش توسط ایجنت واقعاً اجرا و verify شده باشه. درسی که کدش ران نشده، پابلیش نمیشه.
3. ✅ هیچ محتوایی بدون Approve تلگرامی لایو نمیشه.
4. 🔀 جداسازی کامل `content/` (داده) از `apps/` و `pipeline/` (کد) — همون الگوی DEMO/LIVE separation.
5. 👁️⃤ زبان بصری = **آینه‌ی nabulines.com، مونوکروم کامل**: فقط مشکی/سفید/خاکستری — هیچ رنگ اکسنتی در وبسایت و ویدئوها استفاده نمیشه (نارنجی برند مخصوص گرافیک‌های شبکه‌های اجتماعی میمونه). فونت دیسپلی تکنو از خود سایت اصلی بازاستفاده میشه. سایت کاملاً LTR.

---

## 🗺️ معماری کلان (۶ ماژول)

```
┌─────────────────────────────────────────────────────────┐
│  MODULE 6: ORCHESTRATOR (VPS + cron)                    │
│  صف تولید، اجرای روزانه، مانیتورینگ                      │
└────┬────────────────────────────────────────────────────┘
     │
     ├── MODULE 1: CURRICULUM ENGINE ── سرفصل + اسکریپت JSON (EN)
     ├── MODULE 3: VIDEO PIPELINE ───── TTS → Remotion → MP4 + VTT
     ├── MODULE 4: NEWS AGENT ────────── RSS → خبر انگلیسی
     │
     ▼
┌──────────────────────────┐      ┌──────────────────────┐
│ MODULE 5: APPROVAL BOT   │ ───▶ │ MODULE 2: WEBSITE    │
│ (Telegram: تأیید/رد)     │      │ Next.js + CF Pages   │
└──────────────────────────┘      │ ویدئو روی R2         │
                                  └──────────────────────┘
```

**فلوی یک درس:** Curriculum Engine اسکریپت JSON انگلیسی میسازه → Video Pipeline صدا+ویدئو+زیرنویس رندر میکنه → Sandbox Verify کدهای درس رو اجرا میکنه → بات تلگرام پیش‌نمایش میفرسته → تو Approve میزنی → commit + push → Cloudflare Pages خودکار دیپلوی میکنه. **کار تو: یک تپ.**

---

## 📁 ساختار ریپو

```
nabulines-academy/
├── apps/
│   └── web/                      # سایت Next.js (App Router)
│       ├── app/
│       │   ├── page.tsx              # لندینگ
│       │   ├── roadmap/              # مسیر یادگیری + پروگرس
│       │   ├── learn/[chapter]/[lesson]/   # صفحه درس
│       │   ├── news/                 # اخبار
│       │   └── leaderboard/          # امتیازات کامیونیتی
│       └── components/
├── pipeline/
│   ├── curriculum/
│   │   ├── syllabus.json         # سرفصل کامل انگلیسی (source of truth)
│   │   └── prompts/              # پرامپت‌تمپلیت‌های تولید
│   ├── generate-lesson.ts        # Claude API → script JSON (EN)
│   ├── tts.ts                    # ElevenLabs + timestamps
│   ├── capture.ts                # Playwright: ضبط دموی مرورگر
│   ├── verify.ts                 # اجرای sandbox کدهای درس
│   ├── subtitles.ts              # ساخت VTT از timestamps
│   ├── publish.ts                # آپلود R2 + ساخت MDX + commit
│   └── remotion/
│       ├── Root.tsx
│       └── scenes/               # Intro, Slide, Code, Terminal, Browser, Outro
├── content/
│   ├── lessons/                  # MDX نهایی انگلیسی (چیزی که سایت میخونه)
│   ├── scripts/                  # JSON اسکریپت‌ها
│   └── news/                     # MDX اخبار
├── infra/
│   ├── telegram-bot/             # بات تأیید
│   └── cron/                     # جاب‌های VPS
└── MASTER_PLAN.md                # همین فایل
```

---

## 📚 MODULE 1 — Curriculum Engine (سرفصل کامل: از صفر مطلق)

**فرض صفر:** مخاطب هرگز کد ننوشته، GitHub نمیدونه چیه، فقط با گوشی/لپ‌تاپ و شبکه‌های اجتماعی کار کرده. **۴۵ درس، ۸ فصل، هر درس ≤ ۵ دقیقه** (~۳.۵ ساعت محتوای کل).

> جدول‌های زیر مرجع فارسی برای خودت هستن؛ Claude Code موقع ساخت `syllabus.json` عناوین و اهداف رو **به انگلیسی** تولید میکنه.

### فصل ۰ — ذهنیت بیلدر (۴ درس)
| # | درس | خروجی یادگیرنده |
|---|-----|-----------------|
| 0.1 | وایب کدینگ چیه و چرا الان بهترین زمان تاریخه | درک شیفت پارادایم |
| 0.2 | کریتور vs بیلدر — چرا فالوئر کافی نیست | انگیزه + مسیر |
| 0.3 | نقشه‌ی اکوسیستم: Claude، Cursor، v0، Bolt | انتخاب ابزار |
| 0.4 | هنر حرف زدن با AI — اصول پرامپت | ۳ الگوی پرامپت پایه |

### فصل ۱ — راه‌اندازی (۵ درس)
| # | درس |
|---|-----|
| 1.1 | ساخت اکانت Claude — قدم‌به‌قدم |
| 1.2 | تور محیط Claude — چت، Artifacts، پروژه‌ها |
| 1.3 | اولین Artifact: یه چیزی بساز که کار کنه (۶۰ ثانیه‌ای) |
| 1.4 | اکانت GitHub + مفهوم «ریپو» به زبان آدمیزاد |
| 1.5 | اکانت Cloudflare — خونه‌ی آینده‌ی سایت‌هات |

### فصل ۲ — اولین وبسایت (۶ درس)
| # | درس |
|---|-----|
| 2.1 | لندینگ شخصی با Artifacts — از پرامپت تا صفحه |
| 2.2 | پرامپت‌نویسی برای دیزاین: رفرنس، تم، فونت، حس |
| 2.3 | ایتریشن: چطور تغییر بخوای که خراب نشه |
| 2.4 | HTML/CSS چیه؟ (فقط به اندازه‌ای که لازمه *بفهمی*، نه بنویسی) |
| 2.5 | پابلیش اول: از Artifact تا لینک قابل‌اشتراک |
| 2.6 | 🏆 پروژه: لندینگ برند شخصی خودت |

### فصل ۳ — سایت واقعی روی اینترنت (۶ درس)
| # | درس |
|---|-----|
| 3.1 | آپلود پروژه به GitHub (بدون ترمینال — از مرورگر) |
| 3.2 | دیپلوی با Cloudflare Pages — سایتت لایو شد |
| 3.3 | خرید دامنه و اتصال DNS (ساده و تصویری) |
| 3.4 | چندصفحه‌ای کردن سایت |
| 3.5 | فرم تماس و لینک‌های اجتماعی |
| 3.6 | 🏆 پروژه: پورتفولیو کامل روی دامنه‌ی خودت |

### فصل ۴ — Claude Code: ابزار اصلی بیلدر (۷ درس)
| # | درس |
|---|-----|
| 4.1 | Claude Code چیه و چرا گیم‌چنجره |
| 4.2 | نصب: WSL روی ویندوز، قدم‌به‌قدم |
| 4.3 | اولین پروژه با Claude Code |
| 4.4 | فایل‌ها و ساختار پروژه رو بفهم (نقشه‌خوانی) |
| 4.5 | Git با Claude Code: کامیت، برگشت به عقب، نجات پروژه |
| 4.6 | دیباگ: وقتی خراب شد، اینجوری درستش کن |
| 4.7 | 🏆 پروژه: بازسازی سایتت — این بار با Claude Code |

### فصل ۵ — اپ تعاملی (۶ درس)
| # | درس |
|---|-----|
| 5.1 | اپ vs سایت — «State» به زبان ساده |
| 5.2 | اولین اپ: ژنراتور کپشن/هشتگ برای کریتورها |
| 5.3 | ذخیره‌سازی: چرا رفرش همه‌چیو پاک میکنه و راه‌حلش |
| 5.4 | API چیه — درِ ورودی به دنیای داده |
| 5.5 | اتصال به API واقعی: قیمت لحظه‌ای کریپتو |
| 5.6 | 🏆 پروژه: ابزار کریتوری (engagement calculator / link-in-bio) |

### فصل ۶ — ابزار و ایجنت (۶ درس)
| # | درس |
|---|-----|
| 6.1 | ربات تلگرام بساز — بدون نوشتن حتی یک خط کد |
| 6.2 | اتوماسیون: کاری که خودش هر روز انجام میشه (cron) |
| 6.3 | ایجنت چیه — AI که براش کار تعریف میکنی |
| 6.4 | اولین ایجنت: خبرخوان شخصی |
| 6.5 | VPS: کامپیوتر ابری خودت (اجاره، اتصال، اجرا) |
| 6.6 | 🏆 پروژه: ربات/ایجنت شخصی که ۲۴/۷ کار میکنه |

### فصل ۷ — بیلدر حرفه‌ای (۵ درس)
| # | درس |
|---|-----|
| 7.1 | مونتیزیشن: ابزارت رو بفروش (مدل‌ها + پرداخت) |
| 7.2 | لانچ در X: Build in Public به سبک نبولاینز |
| 7.3 | امنیت پایه: API Key، اسکم‌ها، کیف‌پول |
| 7.4 | 🏆 پروژه نهایی: از ایده تا لانچ عمومی |
| 7.5 | بعدش چی؟ نقشه‌ی مسیرهای پیشرفته |

### فرمت `syllabus.json` (انگلیسی)
```json
{
  "chapters": [
    {
      "id": "ch0", "title": "The Builder Mindset",
      "lessons": [
        {
          "id": "ch0-l1",
          "title": "What Is Vibe Coding?",
          "goal": "Understand the paradigm shift and why now is the best time to start",
          "prerequisites": [],
          "maxWords": 750,
          "sceneHints": ["intro", "slide", "browser", "outro"],
          "exercise": "Write one sentence: what's the first thing you want to build?",
          "status": "pending"
        }
      ]
    }
  ]
}
```

### `generate-lesson.ts` — قرارداد خروجی
Claude API (مدل: claude-sonnet-4-6) با پرامپت‌تمپلیت `prompts/lesson.md` اسکریپت انگلیسی تولید میکنه. **اسکیما:**

```json
{
  "lessonId": "ch2-l1",
  "title": "Your First Landing Page with Artifacts",
  "totalWords": 720,
  "scenes": [
    {
      "id": 1,
      "type": "intro",
      "narration": "Conversational English narration for TTS...",
      "visual": { "title": "...", "subtitle": "..." }
    },
    {
      "id": 2,
      "type": "code",
      "narration": "...",
      "visual": { "language": "html", "code": "...", "highlightLines": [3,4] }
    },
    {
      "id": 3,
      "type": "browser",
      "narration": "...",
      "visual": { "captureScript": "capture/ch2-l1-demo.ts" }
    }
  ],
  "quiz": [
    { "q": "...", "options": ["...","...","..."], "answer": 0, "points": 10 }
  ],
  "exercise": "...",
  "verifyCommands": ["npx serve demo && curl -s localhost:3000 | grep '<h1>'"]
}
```

**Validator سخت‌گیر:** اگر `totalWords > 750` یا مجموع تخمین سکانس‌ها > ۳۰۰ ثانیه → reject و regenerate خودکار با فیدبک «کوتاه‌تر کن». لحن نریشن: محاوره‌ای، انرژیک، بدون ژارگون غیرضروری — سبک کریتوری، نه آکادمیک.

---

## 🌐 MODULE 2 — Website (Next.js + Cloudflare Pages)

**استک:** Next.js 14 (App Router, static export) + Tailwind + MDX از `content/` — بدون دیتابیس در فاز اول (پروگرس در localStorage، لیدربرد بعداً روی Cloudflare D1 یا اتصال به سیستم امتیاز موجود نبولاینز). سایت تک‌زبانه انگلیسی، LTR.

### صفحات
| مسیر | محتوا |
|------|-------|
| `/` | لندینگ: هوک **"From Creator to Builder"**، پیش‌نمایش مسیر، CTA شروع رایگان |
| `/roadmap` | ۸ فصل به‌صورت مسیر بصری (مپ)، درصد پیشرفت، قفل ترتیبی درس‌ها |
| `/learn/[ch]/[lesson]` | پلیر ویدئو + زیرنویس EN (قابل خاموش/روشن) + متن کامل درس + کوییز + تمرین + دکمه «Complete» |
| `/news` | فید اخبار AI/وایب‌کدینگ، هر خبر با زاویه **"Why it matters for builders"** |
| `/leaderboard` | امتیاز کوییزها + تکمیل پروژه‌ها (اتصال به کامیونیتی نبولاینز) |

### دیزاین — آینه‌ی nabulines.com، مونوکروم کامل
**مرجع بصری:** خودِ nabulines.com. هیچ رنگی وارد نمیشه.

**توکن‌ها:**
| توکن | مقدار | استفاده |
|------|-------|---------|
| `--bg` | `#000000` | پس‌زمینه‌ی کل |
| `--surface` | `#0a0a0a` | سطح کارت‌ها در hover |
| `--border` | `rgba(255,255,255,0.12)` | خطوط ۱px کارت / آکاردئون / دکمه‌ی ثانویه |
| `--text` | `#ffffff` | تیترها و متن اصلی |
| `--text-2` | `#9ba1a6` | تگ‌لاین، توضیحات |
| `--text-3` | `#6b7280` | فوتر، trust line |

**تایپوگرافی:**
- دیسپلی: **همان فونت تکنوی nabulines.com** (فایل از ریپوی سایت اصلی) — ALL CAPS با letter-spacing باز برای هدر بخش‌ها، به سبک "BUILD SIGNAL, NOT NOISE" → نمونه‌ی آکادمی: "FROM CREATOR TO BUILDER"
- بدنه و UI: Inter — متن درس‌ها و محتوای طولانی **هرگز** با فونت تکنو نوشته نشه (خوانایی)
- عنوان درس‌ها: Inter Bold؛ فونت تکنو فقط برای هدر بخش‌ها، لوگوتایپ و فوتر

**کامپوننت‌ها (دقیقاً مطابق سایت اصلی):**
- دکمه‌ی اصلی: سفید تخت، متن مشکی bold، radius ~۱۴px
- دکمه‌ی ثانویه: شفاف + بوردر خاکستری ۱px، متن سفید، همان radius
- کارت / FAQ: بوردر ۱px، radius ~۱۶px، تاگل «+»
- پروگرس مسیر: ترک خاکستری تیره + فیل سفید
- کوییز: درست/غلط با فیل سفید vs اوت‌لاین + آیکون ✓/✗ (بدون سبز و قرمز)
- Hero: لوگومارک → وردمارک → تگ‌لاین خاکستری → پاراگراف → دو دکمه‌ی عمودی → trust line با جداکننده‌ی «·»

**عنصر امضا:** پترن خطوط منحنی ظریف (SVG با stroke ~۱px و `rgba(255,255,255,0.07)`) پشت hero و بلوک CTA پایانی — همون حس string-art سایت اصلی.

**موشن:** حداقلی — fade/translate ملایم روی اسکرول + hover ظریف دکمه‌ها؛ `prefers-reduced-motion` رعایت بشه. هیچ افکت پر سر و صدایی.

### ویدئو هاستینگ: Cloudflare R2
- باکت `academy-videos` عمومی پشت ساب‌دامین `media.nabulines.com`
- **چرا R2:** egress صفر (استریم ویدئو رایگان)، ۴۵ ویدئو × ~۴۰MB ≈ ۲GB → هزینه‌ی ماهانه < ۱ دلار
- پلیر: `<video>` ساده + track زیرنویس VTT + سرعت پخش + پوستر برند

---

## 🎬 MODULE 3 — Video Pipeline (قلب پروژه)

**زنجیره:** Script JSON → TTS → Capture → Verify → Remotion Render → VTT → R2

### 3.1 صدا — `tts.ts`
- **ElevenLabs مدل `eleven_v3`** — بالاترین کیفیت و بیان؛ انگلیسی قوی‌ترین زبان الون‌لبزه، پس ریسک کیفیت صدا عملاً حذف شده
- endpoint با timestamps (mp3 + زمان‌بندی کاراکتری)؛ فالبک sync: Scribe (STT الون‌لبز) روی خروجی صوت
- هر سکانس جدا رندر میشه → sync دقیق با ویژوال
- **اکشن روز اول:** ۳ صدای نریشن انگلیسی (مرد/زن، انرژیک، آموزشی) رو با یک پاراگراف ثابت تست کن → voice ID برنده در `.env` (`ELEVEN_VOICE_ID`)
- لحن هدف: انرژی کریتوری یوتیوب، نه کتاب صوتی

### 3.2 ویژوال — Remotion (`pipeline/remotion/`)
شش کامپوننت سکانس، همه با **همان توکن‌های مونوکروم سایت** — ویدئو و سایت باید یک محصول واحد حس بشن:
| Scene | رفتار |
|-------|-------|
| `Intro` | تایتل‌کارت انیمیشنی: شماره درس + عنوان + 👁️⃤ |
| `Slide` | بولت‌های انگلیسی با ورود پلکانی، آیکون‌ها |
| `Code` | تایپ‌رایتر کد + هایلایت خطوط + شماره خط |
| `Terminal` | شبیه‌ساز ترمینال: تایپ دستور → خروجی واقعی (از verify.ts) |
| `Browser` | ویدئوی ضبط‌شده Playwright داخل فریم مرورگر برند |
| `Outro` | جمع‌بندی + تمرین + CTA درس بعد |

مدت هر سکانس = مدت فایل صوتی همون سکانس (خوانده‌شده از metadata) → sync خودکار، بدون تنظیم دستی.

### 3.3 ضبط دموی واقعی — `capture.ts`
Playwright هدلس با viewport 1920×1080: اسکریپت دمو رو اجرا میکنه (مثلاً «پرامپت بزن به Claude → Artifact ساخته میشه») و webm ضبط میکنه. **این یعنی بیننده فرایند واقعی رو میبینه، نه اسکرین‌شات فیک** — هم‌راستا با قانون Never-Fabricate.

### 3.4 Verify — `verify.ts`
قبل از رندر نهایی: تمام `verifyCommands` درس در کانتینر sandbox اجرا میشن. Fail → درس برمیگرده به صف regenerate با لاگ خطا. **بدون verify سبز، رندر شروع نمیشه.**

### 3.5 زیرنویس — `subtitles.ts`
از timestamps → گروه‌بندی به خطوط ≤ ۴۲ کاراکتر → خروجی `{lessonId}.en.vtt`. دقت فریم‌به‌فریم، هزینه‌ی تولید ≈ صفر.
💡 در آینده اگر خواستی: افزودن زیرنویس به هر زبان دیگه (ترجمه‌ی VTT با Claude) تقریباً رایگانه و نیازی به دست زدن به ویدئوها نداره — در آرشیو باقی میمونه.

### 3.6 رندر
```bash
npx remotion render Lesson --props=content/scripts/ch2-l1.json out/ch2-l1.mp4
```
1080p، H.264، ~۳۰fps. رندر هر درس ۵ دقیقه‌ای روی VPS معمولی: ۵-۱۵ دقیقه → کل ۴۵ درس ≈ ۷-۱۰ ساعت CPU (cron شبانه). Remotion برای تیم‌های کوچک رایگانه.

---

## 📰 MODULE 4 — News Agent

- **منابع (RSS):** بلاگ Anthropic، OpenAI، Vercel، GitHub Blog، Hacker News (فیلتر AI/dev-tools)
- **cron:** هر ۱۲ ساعت → آیتم‌های جدید → Claude برای هر آیتمِ مرتبط یک خبر انگلیسی ۱۵۰-۲۵۰ کلمه‌ای مینویسه با قالب ثابت:
  1. What happened (فکت، بدون اغراق)
  2. **Why it matters for builders** ← زاویه‌ی امضای آکادمی
  3. لینک منبع
- خروجی MDX در `content/news/` → صف تأیید تلگرام → پابلیش
- فیلتر ضد-نویز: حداکثر ۳ خبر در روز؛ اولویت با چیزهایی که مستقیم روی ورک‌فلوی وایب‌کدینگ اثر دارن

---

## 🤖 MODULE 5 — Approval Bot (Telegram)

همون الگوی Sentinel alerting، این بار دوطرفه (پیام‌های بات به تو فارسی، محتوای زیرش انگلیسی):

```
📗 درس جدید آماده‌ی بازبینی
ch2-l1 — Your First Landing Page with Artifacts
⏱ 4:38 | ✅ Verify passed | 🎬 پیش‌نمایش: <لینک R2 امضاشده>
📄 متن درس: <لینک preview>

[✅ Approve]  [✏️ اصلاح]  [❌ Reject]
```

- **Approve** → `publish.ts`: کامیت MDX + asset ها → push به `main` → Cloudflare Pages خودکار دیپلوی
- **اصلاح** → ریپلای متنی تو (فارسی یا انگلیسی، فرقی نداره — مثلاً «لحنش خشکه، محاوره‌تر») → همون فیدبک میره تو پرامپت regenerate → نسخه v2 برمیگرده به صف
- **Reject** → آرشیو + لاگ دلیل
- دستورات مدیریتی: `/status` (صف تولید)، `/next` (تولید درس بعدی الان)، `/pause`

---

## ⚙️ MODULE 6 — Orchestrator (VPS)

- **State machine** روی `syllabus.json`: هر درس یکی از `pending → scripted → rendered → verified → awaiting_approval → published`
- **cron روزانه (مثلاً ۸ صبح):** ۳ درسِ `pending` بعدی رو از ابتدای صف برمیداره → پایپ‌لاین کامل → ارسال به تلگرام
- **Retry logic:** هر مرحله ۲ بار retry با لاگ؛ شکست نهایی → آلرت تلگرام با traceback
- لاگ ساخت‌یافته JSON در `logs/` + گزارش هفتگی خودکار (چند درس پابلیش شد، هزینه TTS مصرفی)

---

## 🚀 فازهای اجرا

### فاز ۱ — اسکلت + اعتبارسنجی (روز ۱-۳)
- [x] ریپو با ساختار بالا + سایت Next.js با ۵ صفحه (دیتای ماک) — build سبز، static export
- [x] `syllabus.json` کامل ۴۵ درس به انگلیسی (از جدول‌های مرجع همین سند)
- [x] تم مونوکروم کامل (توکن‌ها، دکمه‌ها، پترن string-art، موتیف 👁️⃤، فونت Michroma فالبک)
- [ ] دیپلوی روی Cloudflare Pages + دامنه (مثل الگوی algo-nabulines.com) — نیاز به اکانت CF تو
- [ ] تست ۳ صدای نریشن انگلیسی → انتخاب صدای برند — نیاز به `ELEVEN_API_KEY`
- ✅ **معیار خروج:** سایت لایو با مسیر یادگیری قابل مرور

### فاز ۲ — پایپ‌لاین ویدئو end-to-end (روز ۴-۸)
- [ ] ۶ کامپوننت سکانس Remotion با تم برند
- [ ] `generate-lesson.ts` + validator ۷۵۰ کلمه
- [ ] `tts.ts` + `subtitles.ts` + `capture.ts` + `verify.ts`
- [ ] **درس ch0-l1 به‌صورت کاملاً خودکار تولید بشه** و تو فقط تماشا کنی
- ✅ **معیار خروج:** یک MP4 + VTT قابل‌قبول بدون دخالت دستی

### فاز ۳ — حلقه‌ی تأیید + اخبار (روز ۹-۱۱)
- [ ] بات تلگرام با دکمه‌های Approve/اصلاح/Reject
- [ ] `publish.ts` (R2 + MDX + git push)
- [ ] News Agent + cron روی VPS
- ✅ **معیار خروج:** از موبایل یک درس رو Approve کنی و ۲ دقیقه بعد لایو باشه

### فاز ۴ — تولید انبوه (هفته ۲-۵)
- [ ] Orchestrator: ۳ درس در روز → ۴۵ درس در ~۳ هفته
- [ ] بازبینی روزانه‌ی تو: ~۵-۱۰ دقیقه تپ کردن
- ✅ **معیار خروج:** ۸ فصل کامل پابلیش‌شده

### فاز ۵ — پالیش + لانچ (هفته ۶)
- [ ] کوییز + امتیاز + لیدربرد (اتصال به سیستم امتیاز نبولاینز)
- [ ] SEO انگلیسی + OG image خودکار برای هر درس (تمپلیت برند)
- [ ] در صورت نیاز: زیرنویس‌های زبان دوم (ترجمه‌ی VTT — تقریباً رایگان، بدون رندر مجدد)
- [ ] 🧵 ترد لانچ X + کمپین Nabulines (کوییز "What's the first thing you'll build?")

---

## 💰 هزینه‌ی واقع‌بینانه

| آیتم | هزینه | توضیح |
|------|-------|-------|
| ElevenLabs Creator | $22/ماه | ~۱۰۰ دقیقه TTS/ماه → کل کوریکولوم (~۱۸۰ دقیقه) در ۲ ماه، یا ۱ ماه Pro |
| VPS | $10-15/ماه | همون VPS الگوی Sentinel؛ رندر + cron + بات |
| Cloudflare R2 | < $1/ماه | ~۲GB ویدئو، egress رایگان |
| Claude API (تولید محتوا) | ~$25-40 یکجا | ۴۵ اسکریپت + اصلاحات + اخبار ماه اول |
| دامنه | ~$10/سال | academy.nabulines.com → رایگان روی دامنه موجود |
| Remotion | رایگان | مجوز رایگان برای تیم کوچک |
| **جمع راه‌اندازی** | **~$60-80** | **جاری: ~$35/ماه** |

---

## ⚠️ ریسک‌ها و تصمیم‌های باز

1. **رقابت در بازار انگلیسی‌زبان** — محتوای آموزشی وایب‌کدینگ انگلیسی زیاده؛ تمایز آکادمی: زاویه‌ی کریتور→بیلدر، فرمت میکرو (≤۵ دقیقه)، برند نبولاینز و کامیونیتی/لیدربرد. کیفیت اسکریپت‌ها اینجا حیاتی‌تر از قبله.
2. **دسترسی سازنده** — `*.pages.dev` گاهی در ایران مشکل داره؛ دامنه‌ی کاستوم از روز اول (تجربه‌ی TakWin/Algo Mirror). برای خودت هم Preview ها رو روی دامنه‌ی کاستوم بگیر.
3. **درس‌های UI-heavy** (مثل 1.2 تور محیط Claude) — ضبط Playwright از UI واقعی claude.ai نیاز به لاگین داره؛ راه‌حل: session state ذخیره‌شده یا ضبط یک‌باره‌ی دستی فقط برای این ۳-۴ درس.
4. **به‌روز موندن محتوا** — UI ابزارها عوض میشه؛ Orchestrator هر ۳ ماه یک پاس «content freshness» با re-verify اجرا کنه.
5. **فونت دیسپلی** — فایل فونت تکنو از ریپوی nabulines.com برداشته بشه (لایسنس وب‌ش رو یه بار چک کن)؛ اگر در دسترس نبود، نزدیک‌ترین گزینه‌های رایگان با همون حس: Michroma / Orbitron / Audiowide.
6. **کیفیت نریشن انگلیسی تولیدشده** — اسکریپت‌ها باید native و محاوره‌ای باشن نه ترجمه‌گونه؛ در پرامپت lesson.md لحن "YouTube educator" با مثال صریح قید بشه + اسپات‌چک تو روی ۲-۳ درس اول.

---

## ▶️ پرامپت شروع (کپی کن توی Claude Code)

```
این فایل MASTER_PLAN.md رو کامل بخون. پروژه‌ی nabulines-academy رو از فاز ۱ شروع کن:
1. ساختار ریپو رو دقیقاً طبق سند بساز
2. syllabus.json کامل ۴۵ درس رو بر اساس جدول‌های مرجع سند تولید کن — عناوین، اهداف و تمرین‌ها به انگلیسی native
3. سایت Next.js (static export برای Cloudflare Pages) با ۵ صفحه و دیتای ماک از syllabus — کل UI انگلیسی، LTR
4. تم: کپی دقیق زبان بصری nabulines.com — مونوکروم کامل (مشکی/سفید/خاکستری، بدون هیچ رنگی)، دکمه‌ی اصلی سفید با متن مشکی، ثانویه اوت‌لاین خاکستری، فونت دیسپلی تکنو از ریپوی سایت اصلی (فالبک Michroma)، بدنه Inter، پترن پس‌زمینه‌ی خطوط منحنی ظریف، موتیف 👁️⃤ — جزئیات کامل در بخش «دیزاین» MODULE 2
قوانین سخت سند (۵ دقیقه سقف / ۷۵۰ کلمه، Never-Fabricate، جداسازی content از code) در همه‌ی تصمیم‌ها لازم‌الاجراست.
بعد از هر مرحله چک‌لیست فاز ۱ رو آپدیت کن. سوالی بود، اول بپرس بعد بساز.
```

---
*Nabulines Academy — Build Plan v1.2 | 👁️⃤ From Creator to Builder*
